{
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^15.2.10"
  },
  "scripts": {
    "prepare": "husky install",
    "test": "echo \"No tests specified\""
  },
  "lint-staged": {
    "frontend/**/*.{js,ts,jsx,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "backend/**/*.py": [
      "black",
      "isort"
    ]
  }
}
