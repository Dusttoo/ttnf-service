trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

stages:
  - stage: BuildAndDeployBackend
    jobs:
      - job: BuildBackend
        steps:
          - checkout: self
          
          - task: Docker@2
            inputs:
              containerRegistry: 'ttnfregistry.azurecr.io'
              repository: 'backend'  
              command: 'buildAndPush'
              Dockerfile: 'backend/Dockerfile'
              tags: |
                $(Build.BuildId)

          - task: AzureWebAppContainer@1
            inputs:
              azureSubscription: 'ttnfconnect'  
              appName: 'ttnf-backend'
              containers: |
                container: 'ttnfregistry.azurecr.io/backend:$(Build.BuildId)'  # Point to your backend image in ACR

  - stage: BuildAndDeployFrontend
    jobs:
      - job: BuildFrontend
        steps:
          - checkout: self

          - task: Docker@2
            inputs:
              containerRegistry: 'ttnfregistry.azurecr.io'
              repository: 'frontend' 
              command: 'buildAndPush'
              Dockerfile: 'frontend/Dockerfile'
              tags: |
                $(Build.BuildId)

          - task: AzureWebAppContainer@1
            inputs:
              azureSubscription: 'ttnfconnect'  
              appName: 'ttnf-frontend'
              containers: |
                container: 'ttnfregistry.azurecr.io/frontend:$(Build.BuildId)'  # Point to your frontend image in ACR